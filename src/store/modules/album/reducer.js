const INITIAL_STATE = {
  name: String,
  nb_album: Number,
  picture: String,
  tracklist: String,
  type: String,
};

function album(state = INITIAL_STATE, action) {
  return INITIAL_STATE;
}

export default album;
